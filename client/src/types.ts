export enum LogType {
  ERROR,
  WARNING,
  NORMAL,
  CHECK,
  CONNECTION,
  HAPPNING
}

export enum LogDataType {
  QUERY_CREATED,
  CRAWLING,
  LEVEL,
  MESSAGE,
  ERROR,
}

export enum TabType {
  INTRODUCTION,
  INSTRUCTIONS,
  RESULTS
}
